///Count Vovels is Given String
#include<stdio.h>
#include<conio.h>

int main()
{
       char cSrc[20] ={'\0'};
       int v_Cnt = 0, i=0;

        printf("\n Enter a String:");
        gets(cSrc);

        while(cSrc[i] != '\0')
          {

          }

        printf("\n Count Of Vovels in Given String is=%d.",v_Cnt);

        getch();
        return 0;
 }
